import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'InnovateTech Solutions - Smart Technology for a Better Tomorrow',
  description: 'Leading provider of innovative IoT and smart technology solutions for homes and businesses.',
  keywords: 'IoT, smart home, industrial IoT, technology, automation, sensors, smart building',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
